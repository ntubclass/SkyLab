# 留空 = same-origin，/api 與 /ws 由 vite dev server 代轉到後端
# （見 vite.config.js 的 proxy 設定）。填絕對網址會繞過 proxy，
# 屆時該網址的 origin 必須列進後端 BACKEND_CORS_ORIGINS。
VITE_API_URL=
VITE_DESKTOP_CLIENT_DOWNLOAD_URL=https://github.com/1Ray0/SkyLab-Connect-Releases/releases/latest/download/SkyLab-Connect-Setup.exe
VITE_GOOGLE_CLIENT_ID=
